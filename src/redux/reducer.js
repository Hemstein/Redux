

const initialState={Todo:[
    {id:0, description:'Ahla', isdone:true},
    {id:1, description:'wa', isdone:false},
    {id:2, description:'sahla', isdone:true}
]
}
export const Todoreducer=(state=initialState,action)=>{
switch (action.type) {
    case add:
        return
    case remove:
        return
    case edit:
        return
        
        

    default:
        
}
}